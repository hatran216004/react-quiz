const NotFound = () => {
    return (
        <div className="alert alert-danger text-center mt-3 container">
            404 Not found data with current URL
        </div>
    );
};

export default NotFound;
